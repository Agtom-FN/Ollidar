"""LidarScan cloud job service (Tech Spec §3.8 / workstream D2, MVP).

Single-tenant, one token, one worker, hard upload cap. No payments, no
quotas, no multi-tenancy — those boundaries are contractual for Phase 1.
"""

__version__ = "0.1.0"
