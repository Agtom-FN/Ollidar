{
  "name": "scanengine",
  "version": "0.1.0",
  "description": "LidarScan shared capture/SLAM engine (Tech Spec workstream A).",
  "builtin-baseline": "9e593bb18ea69cc5095e012465dcd675a822ed0d",
  "dependencies": [
    {
      "name": "eigen3",
      "version>=": "3.4.0"
    }
  ],
  "$dependency-onboarding-order": [
    "A1 (now): eigen3 only. The engine core is dependency-light on purpose — every added port must build on all five CI legs, including the macOS universal overlay triplet and the Android NDK triplet.",
    "A7 / A10: gtsam (pose-graph optimization, georef fusion). BEFORE adding it, verify each required transitive Boost component's vcpkg portfile is CMake-based: autotools/configure ports cannot compile-and-run their configure checks for two architectures at once and therefore FAIL under cmake/triplets/universal-osx.cmake. See spikes/s7-windows-toolchain/TOOLCHAIN_NOTES.md.",
    "A7 (optional): ceres-solver, if the LIO refinement wants it alongside GTSAM. Decide in A7.",
    "A8 (decided, NO new dependency): the mount-extrinsics solver was scoped for Ceres (see slam/slam.h's comment) and does NOT use it, nor Eigen. The problem is a single 6-DoF rigid transform, so the normal equations are 6x6 regardless of pose count, and the plane residual's Jacobian is analytic in two lines -- neither Ceres's sparse machinery nor its autodiff has anything to earn there, while ceres-solver drags glog (and optionally SuiteSparse/METIS) across all five CI legs including the universal-osx overlay. src/slam/pushbroom/mount_calibration.cpp ships a ~120-line Levenberg-Marquardt with a hand-written 6x6 LDLT instead; it recovers the extrinsic to 0.000 deg / 6e-13 mm on noise-free data and reproduces every S6 accuracy table (engine/docs/A8-pushbroom.md SS2, SS5). Eigen was declined for the same module so that poses/se3.h stays includable from public headers and ENGINE_WITH_EIGEN=OFF keeps working. Revisit Ceres only for a JOINT solve (extrinsics + time offset + intrinsics across many poses), not for this one.",
    "A3: Livox SDK2 is NOT a vcpkg port. It stays VENDORED and PATCHED (pinned + 3 patches in spikes/s2-mid360-sim/patches/) because stock SDK2's broadcast bind fails with EADDRNOTAVAIL on macOS. Do not swap it for a port without re-running that bind test.",
    "Renderer: Filament is fetched as a prebuilt release by the app layers (S3 pinned v1.75.0), never linked into libscanengine — the engine stays renderer-free and hands out point pages."
  ],
  "$baseline-note": "builtin-baseline is pinned to the same microsoft/vcpkg commit as .github/workflows/engine-ci.yml's VCPKG_COMMIT. Bump both together, never one alone."
}
